\ProvidesPackage{packages/FAU/style}

\usepackage{float}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage[printonlyused]{acronym}
\usepackage{changepage}

% \usepackage{hyperref} % Add in the end

% Packages for arrowlists for item lists of patterns
\usepackage{enumitem}
\newlist{arrowlist}{itemize}{1}
\setlist[arrowlist]{label=$\Rightarrow$}

% Decreases the spacing between itemize items

\let\tempone\itemize
\let\temptwo\enditemize
\renewenvironment{itemize}{\tempone\addtolength{\itemsep}{-0.3\baselineskip}}{\temptwo}

\restylefloat{table}

% School name and location
\university{Friedrich-Alexander-University}
\universitycity{Erlangen}
\universitystate{Bavaria}

% School color found from university's graphic identity document:
% https://www.fau.de/files/2015/10/FAU-Design-manual-Stand-Nov-2013.pdf
\definecolor{SchoolColor}{rgb}{0.000, 0.2196, 0.3961} % FAU_Blue
\definecolor{chaptergrey}{rgb}{0.000, 0.2196, 0.3961} % for chapter numbers

\hypersetup{
    colorlinks,
    citecolor=SchoolColor,
    filecolor=black,
    linkcolor=black,
    urlcolor=SchoolColor,
}

% Formatting guidelines found from Dean's Office
\renewcommand{\frontmatter}{
	\input{frontmatter/personalize}
	\maketitle
	\copyrightpage
	\abstractpage
	\tableofcontents
	\listoffigures % optional 
% 	\listoftables  % optional 
	\dedicationpage
	\acknowledgments
}

\renewcommand{\maketitle}{
%   Page 1, FAU format
	\thispagestyle{empty}
	\vspace*{\fill}
	\vspace{20pt}
	\begin{center}
	\Huge \textcolor{SchoolColor}{\thetitle} \\
	\vspace{20pt}
    \Large \subtitle{}
    \normalsize \\
	\vspace{80pt}
	\textsc{\universityfaculty{} \\
	of the Friedrich-Alexander University \\
    Erlangen-Nürnberg \\
    to \\
    obtain the academic degree \\
    \vspace{20pt}
	\doctoraldegree{} \\
	\vspace{80pt}
	written by \\
	\vspace{20pt}
	\theauthor \\
	\vspace{20pt}
	from \homecityofphd{}
	}
	\end{center} \vspace*{\fill}

%   Page 2, FAU format
	\newpage
    \thispagestyle{empty}
    \vspace*{\fill}
	\vspace{350pt}
	
	\begin{table}[H]
    \begin{tabular}{ll}
    \multicolumn{2}{l}{Approved as a Bachelor Thesis}                              \\
    \multicolumn{2}{l}{from the \universityfaculty{}}                            \\
    \multicolumn{2}{l}{of the Friedrich-Alexander University Erlangen-Nürnberg}   \\
    \multicolumn{2}{l}{} \\
    Reviewer:                                & \firstreviewer{}                \\
                                              & \secondreviewer{}               \\
    \end{tabular}
    \end{table}

	\vspace*{\fill}
}

\newcommand{\copyrightpage}{
	\newpage
	\thispagestyle{empty}
	\vspace*{\fill}
	\scshape \noindent \small \copyright \small 2023\hspace{3pt}-- \theauthor \\
	\noindent all rights reserved.
	\vspace*{\fill}
	\newpage
	\rm
}

\newcommand{\abstractpage}{
	\newpage
	\pagenumbering{roman}
	\setcounter{page}{4}
	\pagestyle{fancy}
	\renewcommand{\headrulewidth}{0.0pt}
	\vspace*{20pt}
	\begin{center}
	\vspace*{100pt}
	\scshape {\Large Abstract} \\ \rm
	\end{center}
	\input{frontmatter/abstract}
	\vspace*{\fill}
	\newpage \lhead{} \rhead{}
	\cfoot{\thepage}
}

\newcommand{\dedicationpage}{
	\newpage \thispagestyle{fancy} \vspace*{\fill}
	\scshape \noindent \input{frontmatter/dedication}
	\vspace*{\fill} \newpage \rm
}

\newcommand{\acknowledgments}{
	\chapter*{Acknowledgments}
	\noindent
	\input{frontmatter/thanks}
	\vspace*{\fill} \newpage
	\setcounter{page}{1}
	\pagenumbering{arabic}
}